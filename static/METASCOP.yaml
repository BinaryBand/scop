title: "Structured CLI Output Protocol (SCOP)"
shortname: SCOP
version_no: "0.3.0"
status: "Draft"

# RFC 7841 metadata
working_group: "Independent Submission"
author: "Shane D. (editor)"
author_affiliation: "Independent"
category: "Informational"
stream: "Independent Submission"
date: "June 2026"
issn: "2070-1721"

copyright_year: "2026"
repository_url: "https://github.com/BinaryBand/scop.git"

terms:
  Producer: "a SCOP-conforming CLI application that emits events."
  Consumer: "software that reads a SCOP event stream and renders it."
  Event: "a single NDJSON line emitted by a producer."
  Stream: "the ordered sequence of events from one command invocation."
  MSGID: "a string identifier classifying an event by its data type (§7)."
  Room: "a page context derived from the subcommand path (§6)."
  Page: "a GUI display unit corresponding to one room, assembled from one or more streams."
  Slot: "a named region in a page layout; events are routed to slots by MSGID family."

principles:
  CLI first: "`msg` MUST always be a complete, human-readable line"
  Standard-grounded: "SCOP MUST NOT conflict with POSIX or GNU; it defers to them"
  Data-typed: "MSGIDs name the data type, not the display form"
  Rooms derived: "Room is always derived from the command path — never declared"
  Zero app knowledge: "A consumer MUST build any page from the stream alone"
  Additive: "Consumers MUST ignore unknown MSGIDs and fields"

abstract: |
  SCOP defines a machine-readable output format for CLI applications that is simultaneously human-readable as plain text and automatically translatable to graphical user interfaces. It composes three existing standards — POSIX.1 Utility Conventions, GNU Coding Standards, and RFC 5424 — adding a typed event vocabulary, a room-based page model, and a three-flag protocol that together enable any conforming CLI application to be rendered as a GUI without application-specific code.

introduction: |
  CLI applications lack a formal output standard. Every CLI-to-GUI bridge must be written per-application, duplicating effort and coupling tightly to application internals. SCOP addresses this by defining structured output that is CLI-first (`msg` always readable as plain text), standard-grounded (builds on POSIX, GNU, RFC 5424), and auto-translatable (a conforming renderer needs no app-specific code).

  SCOP defines a wire format, event vocabulary, room model, GNU flag contracts, and page template. It does not define GUI rendering details, transport beyond stdout, authentication, or application business logic.

  SCOP is a composition, not a replacement:

  | Layer | Standard | Role |
  | --- | --- | --- |
  | Input | POSIX.1 Ch.12 + GNU | flags, subcommands |
  | Envelope | RFC 5424 | event fields, severity |
  | Serialisation | NDJSON | wire format |
  | Output | SCOP | vocabulary + page model |

  The key words "MUST", "MUST NOT", "REQUIRED", "SHALL", "SHALL NOT", "SHOULD", "SHOULD NOT", "RECOMMENDED", "NOT RECOMMENDED", "MAY", and "OPTIONAL" in this document are to be interpreted as described in [RFC2119] and [RFC8174].

security: |
  **Injection.** String values are untrusted. Consumers MUST sanitise before rendering in HTML or injection-sensitive contexts.

  **Denial of service.** Producers MAY emit unbounded events. Consumers SHOULD implement stream size limits.

  **Sensitive data.** Producers MUST NOT include secrets, credentials, or PII in events.

  **Dry run.** Consumers MUST NOT suppress `dry_run: true` annotations. Users MUST be able to distinguish dry-run output from real output.

normative_references:
  - key: RFC2119
    citation: 'Bradner, S., "Key words for use in RFCs to Indicate Requirement Levels", BCP 14, RFC 2119, March 1997.'
    url: https://www.rfc-editor.org/info/rfc2119
  - key: RFC8174
    citation: 'Leiba, B., "Ambiguity of Uppercase vs Lowercase in RFC 2119 Key Words", BCP 14, RFC 8174, May 2017.'
    url: https://www.rfc-editor.org/info/rfc8174
  - key: RFC5424
    citation: 'Gerhards, R., "The Syslog Protocol", RFC 5424, March 2009.'
    url: https://www.rfc-editor.org/info/rfc5424
  - key: POSIX
    citation: 'IEEE, "IEEE Std 1003.1-2024 (POSIX.1-2024)", Issue 8, 2024.'
    url: https://pubs.opengroup.org/onlinepubs/9799919799/
  - key: GNU
    citation: 'Free Software Foundation, "GNU Coding Standards".'
    url: https://www.gnu.org/prep/standards
  - key: NDJSON
    citation: '"Newline Delimited JSON".'
    url: https://ndjson.org

informative_references:
  - key: RFC7841
    citation: 'Halpern, J., Ed., Daigle, L., Ed., and O. Kolkman, Ed., "RFC Streams, Headers, and Boilerplates", RFC 7841, May 2016.'
    url: https://www.rfc-editor.org/info/rfc7841
  - key: LSP
    citation: 'Microsoft, "Language Server Protocol Specification §3.16.1 $/progress", 2021.'
    url: https://microsoft.github.io/language-server-protocol/specifications/lsp/3.17/specification/
  - key: AdaptiveCards
    citation: 'Microsoft, "Adaptive Cards".'
    url: https://adaptivecards.io
  - key: CloudEvents
    citation: 'CNCF, "CloudEvents Specification v1.0.2".'
    url: https://cloudevents.io
  - key: TLDP
    citation: '"Advanced Bash-Scripting Guide — Standard Option Flags", The Linux Documentation Project.'
    url: https://tldp.org/LDP/abs/html/standard-options.html
  - key: gemoji
    citation: 'GitHub, "gemoji — GitHub emoji".'
    url: https://github.com/github/gemoji

sections:
  - title: "Foundation Standards"
    subsections:
      - title: "POSIX.1 and GNU"
        body: |
          Applications SHOULD conform to POSIX.1 Utility Syntax Guidelines (Chapter 12): single-hyphen short options (`-f`), `--` to end option processing, operands following options. Applications SHOULD implement GNU standard flags with defined contracts; each flag's event contract is defined in §8. Flags marked † have no defined contract yet.
        tables:
          - headers: ["Flag", "Short", "Category"]
            rows:
              - ["`--help`", "`-h`", "query"]
              - ["`--version`", "", "query"]
              - ["`--list`", "`-l`", "query"]
              - ["`--status`", "", "query"]
              - ["`--quiet`", "`-q`", "mode"]
              - ["`--verbose`", "`-v`", "mode"]
              - ["`--dry-run`", "`-n`", "modifier"]
              - ["`--recursive`", "`-r`", "modifier"]
              - ["`--force`", "`-f`", "modifier"]
              - ["`--output`", "`-o`", "other †"]
            note: "*† Contract not yet defined; see pending additions.*"

      - title: "RFC 5424 Fields"
        body: |
          Every SCOP event is a serialised RFC 5424 message.

          SCOP encodes `pri` as severity only (0–7). Producers MUST NOT include the facility component in the `pri` value.

          **Required:**

          | Field | Key | Description |
          | --- | --- | --- |
          | PRI | `pri` | Severity level (0–7), encoded as integer |
          | MSGID | `msgid` | SCOP event type (§7) |
          | MSG | `msg` | Complete, human-readable line |

          **Optional:** `ts` (ISO 8601 timestamp), `app` (application name), `pid` (process id).
        subsections:
          - title: "Severity → GUI rendering"
            partial: "partials/severity.md.j2"

  - title: "Wire Format"
    body: |
      Events are serialised as **NDJSON** — one complete JSON object per line, separated by `\n`. Each line MUST be independently parseable. Producers MUST write to stdout. Producers MUST encode all output as **UTF-8**.

      Every event MUST contain `pri`, `msgid`, `room`, and `msg`. The `msg` field MUST be a complete standalone readable line — it MUST NOT require other fields to be meaningful, and MUST NOT verbatim duplicate them.

      ```json
      {"pri": 6, "msgid": "PROCESS_BEGIN", "room": "snapshot", "id": "snap", "label": "Snapshotting ./docs", "total": 142, "msg": "Snapshotting ./docs (142 files)"}
      {"pri": 6, "msgid": "PROCESS_UPDATE", "room": "snapshot", "id": "snap", "current": 71, "total": 142, "msg": "71 of 142: README.md"}
      {"pri": 6, "msgid": "PROCESS_END", "room": "snapshot", "id": "snap", "ok": true, "msg": "Snapshot complete"}
      ```

  - title: "Room Model"
    body: |
      A **room** is derived mechanically from the subcommand path — it is never declared explicitly.

      **Derivation rule:** `room = subcommand tokens joined by "/"`, excluding all flag tokens (`-f`, `--flag`) and all positional operand values. Only the structural subcommand path tokens are included — not the runtime values passed to those subcommands. Root room = `null`.

      | Invocation | Room |
      | --- | --- |
      | `ourapp` | `null` |
      | `ourapp snapshot` | `"snapshot"` |
      | `ourapp snapshot diff` | `"snapshot/diff"` |
      | `ourapp snapshot --list` | `"snapshot"` |
      | `ourapp snapshot diff snap-001 snap-002` | `"snapshot/diff"` |

      The last example shows that positional operand values (`snap-001`, `snap-002`) are excluded. Room strings MUST be stable across versions. Changing a room string is a breaking change.

  - title: "Event Vocabulary"
    partial: "partials/msgids.md.j2"

  - title: "GNU Flag Contract"
    partial: "partials/flag_contracts.md.j2"

  - title: "Page Template"
    partial: "partials/page_template.md.j2"

  - title: "Auto-Translation Rules"
    body: ""
    subsections:
      - title: "Consumer State Model (Informative)"
        body: |
          This section defines a reference state model that consumers SHOULD maintain. Implementations MAY use any internal representation provided routing follows §10. The presentation layer MAY render slot contents in any form.

          #### State Object

          ```proto
          Page {
            room:     string | null
            chrome:   Chrome
            stats:    Map<id, ScalarValue>
            content:  Map<id, Table | List>
            actions:  HelpItem[]
            activity: Map<id, ProcessState>
          }

          Chrome {
            title:     string
            subtitle?: string
            icon?:     string
          }

          ScalarValue {
            id, label, value, type
            unit?:         string
            display_hint?: string
          }

          Table {
            id, label, schema, display_hint?, column_types?
            rows: Map<row_id, values>
          }

          List {
            id, label, ordered
            items: Map<item_id, value>
          }

          HelpItem {
            command, description
            kind?:   "action" | "group"
            params?: Param[]
          }

          ProcessState {
            id, label
            total?:     number
            current?:   number
            ok?:        boolean
            dry_run?:   boolean
            recursive?: boolean
            force?:     boolean
            log:        string[]
          }
          ```

          #### State Transitions

          | Event                                           | Transition                                                         |
          | ----------------------------------------------- | ------------------------------------------------------------------ |
          | `PAGE_BEGIN` — intent `"query"`, same room      | Update `chrome`; all other slots persist unchanged                 |
          | `PAGE_BEGIN` — intent `"query"`, different room | Replace full `Page`; set `room`; clear all slots                   |
          | `PAGE_BEGIN` — intent `"action"`                | No slot change; signals overlay display mode to presentation layer |
          | `SCALAR_SET`                                    | Upsert `stats[id]`                                                 |
          | `SCALAR_CLEAR`                                  | Remove `stats[id]`                                                 |
          | `TABLE_DECLARE`                                 | Initialise `content[id]` as empty `Table`                          |
          | `TABLE_ROW`                                     | Append row to `content[id].rows`                                   |
          | `TABLE_UPDATE`                                  | Update row in `content[id].rows`                                   |
          | `TABLE_END`                                     | Finalise `content[id]`                                             |
          | `LIST_DECLARE` where `id = "help"`              | Reset `actions` list                                               |
          | `LIST_APPEND` where `id = "help"`               | Append to `actions`                                                |
          | `LIST_UPDATE` where `id = "help"`               | Update entry in `actions`                                          |
          | `LIST_REMOVE` where `id = "help"`               | Remove entry from `actions`                                        |
          | `LIST_END` where `id = "help"`                  | Finalise `actions`                                                 |
          | `LIST_DECLARE` — other                          | Initialise `content[id]` as empty `List`                           |
          | `LIST_APPEND` — other                           | Append to `content[id].items`                                      |
          | `LIST_UPDATE` — other                           | Update item in `content[id].items`                                 |
          | `LIST_REMOVE` — other                           | Remove item from `content[id].items`                               |
          | `LIST_END` — other                              | Finalise `content[id]`                                             |
          | `PROCESS_BEGIN`                                 | Initialise `activity[id]`; set `label`, `total`                    |
          | `PROCESS_UPDATE`                                | Update `activity[id].current`, `total`                             |
          | `PROCESS_LOG`                                   | Append `msg` to `activity[id].log`                                 |
          | `PROCESS_END`                                   | Set `activity[id].ok`; finalise                                    |
          | `PAGE_END` — intent `"query"`                   | Stream complete; `Page` persists as-is                             |
          | `PAGE_END` — intent `"action"`                  | Overlay mode ends; `activity` entries retained in `Page`           |
